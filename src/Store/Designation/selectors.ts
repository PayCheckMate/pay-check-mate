import {argsType, DesignationType} from "../../Types/DesignationType";

const selectors = {
    getDesignations: ( state: DesignationType[], args: argsType ) => {
        return state;
    }

}

export default selectors